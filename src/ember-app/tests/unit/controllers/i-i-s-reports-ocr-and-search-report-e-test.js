import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Controller | i-i-s-reports-ocr-and-search-report-e', function (hooks) {
  setupTest(hooks);

  // Replace this with your real tests.
  test('it exists', function(assert) {
    let controller = this.owner.lookup('controller:i-i-s-reports-ocr-and-search-report-e');
    assert.ok(controller);
  });
});
